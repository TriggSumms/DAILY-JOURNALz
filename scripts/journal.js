getEntryData().then(
    ()=>{
    console.log("inside", entryCollection)
    entryList()
    })
    console.log("outside", entryCollection)


// Need to write the function below to allow for entry placement in html

const renderJournalEntries = (entries) => {

    }
    
    // Invoke the render function
renderJournalEntries(journalEntries)