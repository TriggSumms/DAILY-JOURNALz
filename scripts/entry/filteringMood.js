import API from './data.js'



//LIST OF OBJECTIVES:
// Each radio button needs an eventlistener
//-Retreive the value of the radio button via console.log...invoking GetjournalEntries
// Utilize the fileter or forEach methods to extract moods of similar type
//After invoke the function that renders the HTMl representation to the DOM and pass to filtered array of entries


//Starting the process of filtering my Entries by selected mood:
let moodEntrySelector = 

radioButton.addEventListener("click", event => {
    const mood = event.target.value
})
